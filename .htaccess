<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep the project root private while serving Laravel from public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
